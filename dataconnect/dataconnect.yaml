specVersion: "v1beta"
serviceId: "autismapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "autismapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
